apiVersion: 1
contactPoints:
  - orgId: 1
    name: Telegram
    receivers:
      - uid: telegram-receiver
        type: telegram
        settings:
          bottoken: ${TELEGRAM_BOT_TOKEN}
          chatid: "456104186"
          message: |
            🚨 Antifraud Alert
            {{ range .Alerts }}
            {{ .Annotations.summary }}
            {{ end }}
